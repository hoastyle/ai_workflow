# Makefile 使用指南

本项目使用 **Make** 作为统一的开发工作流工具，简化常见操作。

## 快速开始

```bash
# 安装到 ~/.claude/
make install

# 验证安装
make verify

# 查看所有可用命令
make help
```

## 命令分类

### 📦 安装和卸载

| 命令 | 说明 |
|------|------|
| `make install` | 标准安装（推荐） |
| `make install-link` | 符号链接安装（开发模式） |
| `make install-no-backup` | 无备份安装 |
| `make uninstall` | 卸载 |

### 🔍 验证和检查

| 命令 | 说明 |
|------|------|
| `make verify` | 验证安装完整性 |
| `make verify-manifest` | 验证清单一致性 |
| `make lint` | 检查脚本语法和质量 |

### 📚 文档管理

| 命令 | 说明 |
|------|------|
| `make docs-validate` | 验证 Frontmatter 元数据 |
| `make docs-index` | 更新文档索引 |
| `make docs-graph` | 生成文档关系图 |
| `make docs-check` | 全面检查文档 |

### 🧹 项目维护

| 命令 | 说明 |
|------|------|
| `make clean` | 清理临时文件 |
| `make format` | 格式化脚本（需要 shfmt） |
| `make status` | 显示项目状态 |
| `make help` | 显示帮助信息 |

## 特点

✅ **零依赖** - Make 是标准工具，几乎所有系统都预装

✅ **易于学习** - `make help` 立即了解所有命令

✅ **任务编排** - 自动管理任务依赖（如 `make docs-check` = `make docs-validate` + `make docs-index`）

✅ **与现有脚本集成** - 统一封装 shell 脚本和 Python 工具

✅ **易于扩展** - 添加新命令无需修改代码

## 工作流示例

### 新用户安装

```bash
$ cd ai_workflow
$ make install      # 安装
$ make verify       # 验证
$ /wf_03_prime      # 加载项目上下文
```

### 开发者日常工作

```bash
$ make install-link     # 开发模式（使用符号链接）
$ make status           # 查看项目状态
$ make lint             # 检查代码质量
$ make docs-check       # 验证文档
```

### 项目维护

```bash
$ make verify-manifest  # 验证清单一致性
$ make clean            # 清理临时文件
$ make format           # 格式化脚本（可选）
```

## 为什么使用 Make？

参考项目咨询分析：[Ultrathink 架构分析](/home/howie/Software/utility/Reference/SuperClaude_Framework)

**核心优势**：
1. 业界标准，零依赖
2. 对 Python 项目完美支持
3. 强大的任务编排能力
4. 与 CI/CD 天然契合
5. 简化复杂工作流

## 注意事项

- Make 需要使用 **Tab** 缩进（不能用空格），Makefile 已正确配置
- Windows 用户需要在 WSL 或 Git Bash 中运行
- 某些命令（如 `make format`）需要额外工具（shfmt）

## 更多帮助

```bash
# 查看所有可用命令和说明
make help

# 查看完整的项目安装文档
cat INSTALL.md

# 查看项目概览
cat README.md
```
